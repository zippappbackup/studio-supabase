# This setting prevents npm from creating nested node_modules folders,
# which can help with dependency resolution and reduce path length issues.
# It hoists packages to the top level whenever possible.
legacy-peer-deps=true
